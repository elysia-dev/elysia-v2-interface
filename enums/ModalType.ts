enum ModalType {
  Connect = 'Connect',
  Disconnect = 'Disconnect',
  Staking = 'Staking',
  Claim = 'Claim',
  NoAccount = 'NoAccount',
  ConfirmEnded = 'ConfirmEnded',
}

export default ModalType;
